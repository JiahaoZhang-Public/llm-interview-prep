# English Solutions Branch

- This branch is reserved for reference implementations, walkthroughs, and answer notes.
- Keep `starter.py`, `test_public.py`, and `src/llm_prep/**` interface-compatible with the practice branches.
- Prefer adding answers one problem at a time instead of dropping a full leaked bank into the repo.
- If you add written explanations, create `solution_notes.md` inside the relevant problem directory instead of overwriting `problem.md`.
